apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- dashboard-4-maas-token-metrics-admin.yaml
- maas-cost-rates.yaml
# No Limitador ServiceMonitor here. COO already scrapes via MaaS
# ServiceMonitor/limitador-metrics in redhat-ods-monitoring.
# The 3.4 UWM fallback (limitador-servicemonitor.yaml) must not be applied on 3.5.
