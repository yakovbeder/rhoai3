apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- dashboard-4-maas-token-metrics-admin.yaml
- maas-cost-rates.yaml
# limitador-servicemonitor.yaml is optional — see README.md (do not add here).
